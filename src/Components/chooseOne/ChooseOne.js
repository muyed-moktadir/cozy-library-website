import React from 'react';

const ChooseOne = ({name}) => {
    console.log(name)
    return (
        <div>
            <h3>authothors name:{name}</h3>
        </div>
    );
};

export default ChooseOne;